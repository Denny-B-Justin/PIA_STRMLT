# ── Databricks connection ─────────────────────────────────────────────────────
# Hostname only — no https:// prefix, no trailing slash
DATABRICKS_SERVER_HOSTNAME= adb-6102124407836814.14.azuredatabricks.net

# HTTP path of the SQL Warehouse
# Found at: SQL Warehouses → your warehouse → Connection details
DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/ab3edf3f6876159d

# OAuth M2M service principal credentials
# Create at: Settings → Identity & Access → Service Principals → Add
DATABRICKS_CLIENT_ID= a8a8db79-8e75-4dcd-b321-de8ad6018e25
DATABRICKS_CLIENT_SECRET= dose4fc18fadc405f8a875f8a109efcd1671

# ── Flask / Dash ──────────────────────────────────────────────────────────────
# Any long random string — used to sign session cookies
SECRET_KEY= a3f8c2e1d4b6e9f0a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4

# ── Auth (optional) ───────────────────────────────────────────────────────────
# Set to True to enable flask-login authentication gate
AUTH_ENABLED=False

# ── Schema overrides (optional — defaults shown) ──────────────────────────────
ZAMBIA_CATALOG=prd_mega
FACILITIES_SCHEMA=sgpbpi163
RESULTS_SCHEMA=sgpbpi163

# ── Query cache tuning (optional — defaults shown) ────────────────────────────
QUERY_CACHE_TTL_SECONDS=300
QUERY_CACHE_MAX_ENTRIES=256